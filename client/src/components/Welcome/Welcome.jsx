import React from 'react';
import { Typography } from '@mui/material';

const style = {
    bottom: '11%',
    left: '3%',
    fontWeight: 'normal',
    fontSize: '120%',
        
  };

const Welcome = () => {
  return <Typography style={style} >Welcome to Evntya</Typography>;
};

export default Welcome;
